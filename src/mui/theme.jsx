import { createTheme } from "@mui/material";

export const theme = createTheme({
    typography : {fontFamily : `"Yekan", "Roboto"`},
    direction : "rtl"
});

//change the default variable of material ui